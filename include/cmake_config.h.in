
#if defined(WIN32)
# ifndef NDEBUG
#  pragma comment(linker, "/SUBSYSTEM:CONSOLE")
# else
#  pragma comment(linker, "/SUBSYSTEM:WINDOWS")
#  define WIN32_LEAN_AND_MEAN
# endif
#endif

/* versioning */
#define AAXSUPPORT_MAJOR_VERSION @AAXSUPPORT_MAJOR_VERSION@
#define AAXSUPPORT_MINOR_VERSION @AAXSUPPORT_MINOR_VERSION@
#define AAXSUPPORT_MICRO_VERSION @AAXSUPPORT_MICRO_VERSION@
